name: RunApp

on:
  push:
    branches:
      - master

jobs:
  run:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Build and run Docker image
        run: |
          docker build -t runDocker -f .github/Dockerfile .
          docker run runDocker
